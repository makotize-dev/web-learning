# Web学習ログ

---

## Day 1 — HTML基礎（Stage 1）

### 学んだこと

**1. HTMLタグは「意味」を表す**
- タグは暗記するものではなく、「これは何を意味するか」から選ぶ
- 例：`<p>` は「段落」→ 段落は独立する → だからブロック要素

**2. ブロック要素とインライン要素**

| 種類 | 挙動 | 例 |
|---|---|---|
| ブロック要素 | 縦に積まれる（行全体を占める） | `<div>` `<p>` `<h1>`〜`<h6>` `<section>` |
| インライン要素 | 横に並ぶ（文字の流れに乗る） | `<span>` |

- インライン要素は「同じ行の流れ」に乗る
- 前の要素がブロック要素なら行が終わっているので、次の行から始まる

**3. 構造タグの役割**

| タグ | 意味 |
|---|---|
| `<header>` | ページや記事のヘッダー |
| `<main>` | メインコンテンツ |
| `<section>` | 意味的にまとまった1つのかたまり |
| `<div>` | 意味なし・ブロック用のまとめ役 |
| `<span>` | 意味なし・インライン用のまとめ役 |

**4. 手を動かして確認したこと**
- `<p>`タグを追加してプレビューで確認
- `<div>`と`<span>`を並べてブロック/インラインの違いを目視確認
- `<span>`に変えたときに前の`<p>`の影響で行が変わることを確認

---

## Day 2 — HTMLテーブルと構造の深掘り

### 学んだこと

**1. テーブルのタグ体系**

| タグ | 意味 | レベル |
|---|---|---|
| `<table>` | テーブル全体 | 外枠 |
| `<thead>` | 見出し行グループ | 行グループ |
| `<tbody>` | データ行グループ | 行グループ |
| `<tr>` | 1行 | 行 |
| `<th>` | 見出しセル | セル |
| `<td>` | データセル | セル |

**2. `<thead>`と`<tbody>`を分ける理由**
- スクロール時に見出しを固定できる
- 印刷時に各ページ先頭に見出しを繰り返せる
- スクリーンリーダーが構造を正しく読み上げられる
- 見た目のためではなく「意味」のために書く → 結果として機能が良くなる

**3. `<div>`と`<span>`の本来の役割**
- 複数の要素をひとまとめにして、CSSやJSで一括して扱えるようにする入れ物
- ブロック/インラインの違いはその副産物

---

## Day 3 — CSS基礎（Stage 2）

### 学んだこと

**1. HTMLとCSSの分離**
- 同じHTMLで見た目だけを変えられる
- 構造と見た目を分けることで分業・変更が容易になる

**2. CSSプロパティの基本**

| プロパティ | 意味 |
|---|---|
| `font-size` | 文字の大きさ |
| `font-weight` | 文字の太さ |
| `color` | 文字色 |
| `background` | 背景色 |
| `padding` | 内側の余白 |
| `border-radius` | 角の丸み（高さの半分を超えると変化しなくなる） |
| `border` | 枠線（太さ・種類・色をまとめて指定） |

**3. セレクタ**

| CSSの書き方 | 対応するHTML | 意味 |
|---|---|---|
| `.badge` | `class="badge"` | このクラスを持つ全要素 |
| `h2` | `<h2>` | このタグ全部 |
| `#title` | `id="title"` | このIDを持つ要素 |

**4. 複数クラスの使い方**
- `class="badge badge-blue"` のように複数指定できる
- 共通スタイルを `.badge` に、差分を `.badge-blue` に分けて重複を避ける

---

## Day 4 — CSS実践（ボックスモデル・Flexbox）

### 学んだこと

**1. paddingとmarginの違い（手を動かして確認）**
- `padding` → 要素の内側の余白（文字と枠の間）
- `margin` → 要素の外側の余白（要素と要素の間）
- `margin-bottom: 24px` のように方向を個別指定できる
- 指定しない方向は 0（全方向に広がるわけではない）

**2. Flexboxの仕組み（手を動かして確認）**
- `display: flex` は親に書く → 子の並び方を親が制御する
- `display: block` に変えると子のスパンはインラインのまま横並びになる
- 子に `display: block` を追加すると縦になるが横幅が行全体に広がる
- `gap` で子要素同士の隙間を指定できる

### 進捗メモ
- 出張中（4/9〜4/14）のスマホ学習でCSS・JSの概念を先取り
- スケジュールより約1週間前倒し。Level A達成（4月末）は問題なし

---

## Day 5 — CSSカスケード・詳細度・子孫セレクタ

### 学んだこと

**1. 詳細度の確認（手を動かして確認）**
- `h2 { color: red }` を追加 → Stage 1のh2は赤になった、Stage 2のh2はならなかった
- `.preview-s2 h2`（クラス+タグ）が `h2`（タグのみ）に勝つ

**2. 子孫セレクタ**
- `.preview-s2 h2` → 「.preview-s2の中にあるh2」という意味
- スペース区切りは「入れ子の指定（子孫セレクタ）」
- 範囲を絞らないと複数エリアのh2すべてに効いてしまう

**3. CSSの基本原則まとめ**
- セレクタ → 「どこに効かせるか」の絞り込み
- 詳細度 → 「競合したときどちらが勝つか」の優先度
- この2つが分かればCSSのほとんどの問題は解決できる

---

## Day 6 — JS実践（DOM・イベント・classList）

### 学んだこと

**1. タブ切り替えのコードを読み解いた**
- `addEventListener('click', ...)` でイベント待ち受け
- `querySelectorAll` で複数要素を選択
- `classList.remove` で全部リセット → `classList.add` で1つ有効化

**2. `data-*` 属性**
- `data-tab="tab-def"` のようにHTMLに独自データを埋め込める
- JSから `btn.dataset.tab` で取得できる
- ボタンとパネルをつなぐ「橋渡し」として使う

**3. `toggle` vs `remove/add`**
- `toggle` → 開閉を繰り返す場合（ツールチップ）
- `remove` + `add` → 必ずリセットしてから1つ有効化する場合（タブ）

**4. 自分でバッジを追加した（HTML + JS + CSS）**
- HTMLにバッジとツールチップのペアを追加
- JSの配列に `['b-sc','tip-sc']` を追加
- CSSに `badge-yellow` のスタイルを追加
- 3つのファイル（実際は1ファイル内の3箇所）を連携させて動作確認

---

## Day 7 — Level A実践：profile.html制作開始

### やったこと
- `profile.html` を新規作成
- HTML骨格を自分で書いた（header / main / 5つのsection）
- `<h1>` と `<h2>` の使い分けを修正できた
- CSSを `<style>` タグで追加（bodyの背景色・文字色、headerの背景・padding、sectionのmargin）

### 現在のprofile.htmlの状態
- 背景：`#faf8f5`（オフホワイト）
- 文字色：`#666666`（薄めグレー）
- ヘッダー：yellowgreen背景 + padding
- セクション間：margin-bottomで間隔あり

---

## Day 8 — profile.htmlデザイン完成・GitHub公開

### やったこと
- `<main>` にpadding追加
- `<h2>` に左ボーダーと`padding-left`でアクセントをつけた
- Google Fonts（Noto Sans JP）を導入
- 色を自分で調整（オレンジ背景・黄色ボーダー・濃いグレー文字）
- `<link>` タグを `<style>` の前に移動（慣習に合わせた）
- `<meta name="viewport">` を追加してスマホ対応
- GitHubにアップロードしてスマホで表示確認

### 現在のprofile.htmlの状態
- viewport対応済み・スマホで見やすい
- フォント：Noto Sans JP
- 背景：#faf8f5、文字：#434242
- ヘッダー：rgb(234,122,11)のオレンジ
- h2：黄色の左ボーダー

---

## Day 9 — Level A達成

### やったこと
- `<a href="URL">テキスト</a>` でリンクを追加（hrefなしでは飛ばないことを確認）
- `<img src="URL" alt="説明">` で画像を追加
- GitHubにアップロードしてスマホで最終確認

### Level A 達成 ✓（2025年4月末目標 → 達成）
- HTMLの構造を書ける
- CSSで色・フォント・余白を変えられる
- リンク・画像を追加できる
- スマホ対応（viewport）できる
- GitHubで公開できる

---

## Day 10 — スケジュール更新・ガントチャート改良

### やったこと
- ガントチャートに完了済みタスク（斜線スタイル）・今日の縦線を追加
- Level A達成バッジをschedule.htmlに追加
- Level B: 4/21〜5/31（約6週間）に設定
- Level C: 6/1〜6/30（1ヶ月）に設定
- タイムラインを6月末に短縮

### スケジュール（確定版）
- Level A: 4/6〜4/20 ✓ 達成
- Level B: 4/21〜5/31（CSS Grid・レスポンシブ → 複数ページ構成・フォーム → 実践）
- Level C: 6/1〜6/30（JS応用・CSS アニメーション → 本番サイト制作）

### 次回の続き
- Level B開始：CSS Gridから

---

## Day 11 — CSS Grid・メディアクエリ（Level B開始）

### 学んだこと

**1. CSS Gridの基本**

| 概念 | 書き方 | 意味 |
|---|---|---|
| Grid宣言 | `display: grid` | 親が格子を制御 |
| 列の設計図 | `grid-template-columns` | 何列・何の幅か |
| 分数単位 | `1fr` | 余白を比率で分配 |
| 繰り返し | `repeat(2, 1fr)` | 同じ列定義をN回 |
| 流動列 | `repeat(auto-fill, minmax(160px, 1fr))` | 入るだけ詰め込む |

**2. FlexboxとGridの使い分け**
- Flexbox → 1軸（横or縦）、一列のアイテム並び
- Grid → 2軸（横と縦）、格子状の配置

**3. メディアクエリ**
- `@media (max-width: 600px) { }` → 600px以下のときだけCSSを上書き
- Gridの `auto-fill` はベースルールが生きたまま → 幅が広いほど列が増えた
- 「PCは2列固定・スマホは1列」にするには：ベースを `repeat(2, 1fr)` + `@media` で `1fr` に上書き

**4. 手を動かして確認したこと**
- `1fr 1fr` → `2fr 1fr` に変えて幅の比率を確認
- テーブルの代わりにカードグリッドを実装
- `auto-fill` の「列が自動で増減する」挙動を確認
- `@media` 追加後に `auto-fill` が600px以上で生き続けることを発見・修正

### 次回の続き
- CSS Grid続き：`grid-template-areas` または Flexboxとの組み合わせ

---

## Day 12 — CSS Grid続き（`grid-template-areas`・親子関係）

### 学んだこと

**1. `grid-template-areas`**
- 親に「地図」を描く：`grid-template-areas: "explanation preview"`
- 子に「自分はどのエリアか」を宣言：`grid-area: explanation`
- 数字（`1fr 1fr`）より意図が読みやすい
- 同じ名前を複数セルに書くとその要素が複数列/行を占有する

**2. GridのGrand Unified Rule：親と子**
- `display: grid` を書いた要素が**親（Gridコンテナ）**
- その直下のタグが**子（Gridアイテム）**
- Gridは親と子の間にだけ働く。孫には届かない
- `.coin-grid`（親）→ `.coin-card`（子）で確認済み

### 次回の続き
- 複数ページ構成

---

## Day 13 — 複数ページ構成

### 学んだこと

**1. ページ間リンク**
- `<a href="schedule.html">` で同じフォルダ内のHTMLに移動できる
- 自分のページへのリンクは不要

**2. `<nav>` タグ**
- ナビゲーションを意味するタグ
- `<header>` の中に入れて使う

**3. 共通部分の重複問題**
- HTMLだけでは外部ファイルから読み込めない
- Level Bの現実解：各ファイルにコピーして書く
- Level C以降：JS（fetch）やフレームワークで解決

**4. CSSの追加知識**
- `text-decoration: none` → リンクの下線を消す
- `nav a`（子孫セレクタ）→ nav内のaだけに絞って適用（Day 5の復習）

**5. 手を動かして確認したこと**
- `profile.html` と `schedule.html` に `<nav>` を追加
- 両方向のページ移動を確認

### 次回の続き
- フォーム（`<input>` / `<button>`）

---

## Day 14 — フォーム

### 学んだこと

**1. フォームの基本タグ**

| タグ・属性 | 役割 |
|---|---|
| `<form>` | フォーム全体の器 |
| `<input type="...">` | 1行入力欄。typeで種類が変わる |
| `<textarea rows="4">` | 複数行入力欄 |
| `<label>` | 入力欄の説明 |
| `<button type="submit">` | 送信ボタン |

**2. `type` 属性の種類と二重の役割**
- `text` / `email` / `password` / `number` / `checkbox`
- 見た目を変える（passwordは文字が隠れる）
- バリデーションも行う（emailは@がないと送信できない）

**3. バリデーション属性**
- `required` → 空のまま送信するとブラウザがブロックしてメッセージを表示
- JavaScriptなしで動く

**4. フォームの限界**
- `action`（送信先）と `method`（GET/POST）が必要
- データを受け取って処理するにはサーバーが必要
- Level Bでは「見た目と構造」まで

**5. CSSで確認したこと**
- `cursor: pointer` → マウスを乗せると指マークになる
- `padding` でボタンのクリック領域を広げる

### 次回の続き
- Level B実践：これまでの知識を使って新しいページを作る

---

## Day 15 — Level B実践：contact.html制作

### やったこと
- `contact.html` を一から自力で作成
- HTML骨格・CSS・フォームをすべて自分で記述
- `profile.html` / `schedule.html` / `contact.html` の3ページナビを完成

### 自力で書けたもの
- `<!DOCTYPE html>` からの基本構造
- `<head>` のmeta・viewport・Google Fonts
- `<header>` + `<nav>` + `<main>` + `<section>` の構造
- フォーム（input・textarea・required）
- CSS（body・header・nav a・button）

### 次回の続き
- Level B総仕上げ or Level C（JS応用・アニメーション）へ

---

## Day 16 — CSSトランジション

### 学んだこと

**1. `:hover` 疑似クラス**
- `button:hover { }` → マウスを乗せたときだけ適用されるスタイル
- `nav a:hover { }` と `nav:hover { }` は別物。対象の要素を正確に指定する

**2. `transition` プロパティ**
- `transition: プロパティ名 時間` → 何を何秒でなめらかに変化させるか
- `transition: background 0.3s` → 背景色だけ0.3秒で変化
- `transition: all 0.3s` → `:hover` で変化するプロパティすべてに適用

**3. セットで使う**
- `:hover` は「変化を定義する」
- `transition` は「その変化をなめらかにする」
- 2つは別の役割でセットで機能する

**4. ボタンの浮き上がり効果**
- `transform: translateY(-3px)` → Y軸マイナスで上方向に移動
- `box-shadow: 0 6px 16px rgba(0,0,0,0.2)` → 影で「浮いている」感を強調
- `transition: all 0.3s` にしておけば後から追加したプロパティも自動でなめらかになる

### 次回の続き
- 既存ページ（profile.html）の改善（ユーザーが改善案を考えてくる）

---

## Day 17 — 既存ページ改善（ラジオボタン・背景模様）

### やったこと

**1. contact.html にフリガナ入力欄を追加**
- `input type="text"` の復習

**2. profile.html に5段階ラジオボタンを追加**
- `type="radio"` → 1つだけ選択できる入力
- `name` 属性が同じ要素がグループになり、グループ内で1つだけ選択できる
- `<div>` で各セットを包んで縦並びに

**3. profile.html にドット背景を追加**
- `background-image: radial-gradient(circle, #ccc 1px, transparent 1px)`
- `background-size: 20px 20px` → タイルの大きさ＝ドットの間隔
- 同じ値2つで境界くっきり、違う値にするとグラデーション

**4. 気づき**
- スペルミス1文字（`trnsparent`）でCSSが丸ごと無効になる
- ブラウザは間違いを黙って無視する → 「変わらない」時はスペルを疑う

### 次回の続き
- Level B総仕上げ or Level C開始

---

## Day 18 — Level C開始：JS基礎（Stage 3）・非同期処理（Stage 4）

### 学んだこと

**1. JSの3ステップ（Stage 3）**

| ステップ | コード | 役割 |
|---|---|---|
| ① いつ | `addEventListener('click', ...)` | イベントを待ち受ける |
| ② どこ | `document.querySelectorAll(...)` | 操作する要素を選ぶ |
| ③ 何を | `classList.remove/add('active')` | 状態を変える |

- JSはクラスを付け外しするだけ。見た目の変化はCSSに委ねる
- HTMLとCSSは静的（ユーザーの動きに反応しない）。JSが「動き」を担う

**2. DevToolsで実確認したこと**
- Elements タブでタブボタンを選択しながらタブを切り替えると、`class` 属性に `active` が出たり消えたりするのを目視確認
- JSがクラスの文字を操作 → CSSのルールが自動で適用 という流れを確認

**3. 非同期処理（Stage 4）**

| キーワード | 意味 |
|---|---|
| `fetch(url)` | サーバーにデータを取りに行く |
| `await` | Promiseの解決（データの到着・変換の完了）を待つ |
| `async` | この関数の中で `await` を使っていいという宣言 |
| `try` | 処理を一度試みる |
| `catch` | 失敗（エラー）したときの受け皿 |

- JSはシングルスレッドなので、通信を待ちながら止まるとフリーズする
- `async/await` のおかげで「待ちながら他のことができる」
- `res.json()` にも `await` が必要な理由：通信だけでなく変換も時間がかかるため
- `try/catch` は「捕まるまで繰り返す」ではなく「1回試みて失敗したら対処する」

**4. Promiseとは**
- サーバーとの通信は時間がかかる → 即座にデータが届かない
- `fetch()` は即座に **Promise**（「後で届く約束」）を返す
- `await` でその Promise の解決を待つ

### 次回の続き
- Stage 5：CSSアニメーション（@keyframes・transform・transition）

---

## Day 19 — CSSアニメーション（Stage 5）

### 学んだこと

**1. `@keyframes` の仕組み**
- `0%` `65%` `100%` のように「タイミング（%）と状態」をコマ撮りで定義する
- 定義しただけでは動かない。`animation` プロパティで要素に適用して初めて動く

**2. `animation` プロパティ**

```css
animation: 名前  時間   イージング;
           ↑      ↑      ↑
         @keyframesの名前  何秒で  どんな加減速で
```

- `price-up` は「価格を上げる」という意味ではなく、`@keyframes price-up { }` の**名前を指定**しているだけ

**3. `transform` と `transition` の役割の違い**

| プロパティ | 役割 | 例 |
|---|---|---|
| `transform` | **何を**変形するか | `translateY(-4px)` `scale(1.08)` `rotate(10deg)` |
| `transition` | その変化を**どう**見せるか | `0.15s ease` でなめらかに |

**4. JSとの関連**
- Stage 3 と同じパターン：JSはクラスを付け外しするだけ、アニメーションの実行はCSSに委ねる
- ボタンクリック → JSが `.up` クラスを追加 → CSSの `animation: price-up` が発動

### Stage 3〜5 の共通原則
> **JSはクラスを操作する。見た目・動き・アニメーションはCSSに委ねる。**

### 次回の続き
- 本番サイト制作（Stage 3〜5 の知識を使った実践）

---

## Day 20 — 本番サイト制作：prices.html（fetch実装）

### やったこと

**1. prices.html を自力で構築**
- HTML骨格・viewport・Google Fonts・`<style>`タグを自分で判断して記述
- テーブル構造（`<thead>` / `<tbody>`）を選択（価格・変化率・銘柄は表形式が適切と判断）
- `id="price-body"` / `id="status"` / `id="fetch-btn"` を適切に配置

**2. CoinGecko API から実データを取得**
- CORS制限のない CoinGecko API を使用（取引所の直接APIはCORS制限あり）
- `fetch` → `res.json()` → `console.log(data)` でデータ構造を確認
- `data.bitcoin.jpy` のようにネストしたオブジェクトへのアクセスを確認

**3. テーブルへの動的描画**
- `coins.map(...)` で4通貨分の `<tr>` を生成
- `body.innerHTML = ...` でDOMに挿入
- `toLocaleString()` で価格をカンマ区切りに、`toFixed(2)` で変化率を小数2桁に整形
- ボタンクリックで実際の価格データがテーブルに表示されることを確認

### 次回の続き
- CSS でスタイリング
- 変化率をプラス（緑）・マイナス（赤）で色分け（classList × CSS）
- 行挿入時のアニメーション（Stage 5の応用）

---

## Day 21 — prices.html 続き（色分け・JSとCSSの関係）

### やったこと
- 変化率をプラス（緑）・マイナス（赤）で色分け実装
  - JSで `changeClass = change >= 0 ? 'up' : 'down'` と条件分岐
  - `<td class="${changeClass}">` でDOMに反映
  - CSSの `.up` `.down` が自動で適用されることを確認

### 重要な気づき：JSとCSSの関係
- CSSはJSのコードを見るのではなく、**JSが作ったDOMを見る**
- JSが `<td class="up">` を生成 → DOMに挿入 → CSSの `.up { }` が当たる
- 「JSがどんなDOMを作るか想像しながらCSSを書く」という開発の順番を理解
- DevTools の Elements タブ = JSが作ったDOMをリアルタイムで確認する道具

### 次回の続き
- CSS でスタイリング（body・table・th・td など）
- 行挿入時のアニメーション（@keyframes の応用）

---

## Day 22 — prices.html CSS styling・行アニメーション

### やったこと

**1. テーブルのCSS styling（自力で記述）**
- `border-collapse: collapse` → セル間の二重線を解消
- `th, td { border: 1px solid #ccc; font-size: 20px; text-align: right; }` → 枠線・文字サイズ・右寄せ
- `th { background-color: orange; text-align: center; }` → ヘッダーだけ別スタイル
- 数字は右寄せ・ラベルはセンター という意図的な設計判断

**2. `@keyframes` で行アニメーション実装**
- `fadeSlideIn`：`opacity: 0` + `translateY(10px)` → `opacity: 1` + `translateY(0)` で下から上へフェードイン
- `td { animation: fadeSlideIn 0.4s ease; }` で適用

### 詰まったポイントと解決
- `td { animation: ... }` を `@keyframes` の中に書いてしまった → `@keyframes` はアニメーションの「動き」だけを定義する場所。セレクタは外に出す
- `tr` に `transform` が効かない → `display: table-row` の制約。`td` に変更して解決

### 学んだこと
- `translateY` のプラス/マイナスの方向（プラス=下、マイナス=上）
- `@keyframes` はアニメーションの「定義」。`animation` プロパティで「適用」して初めて動く
- `:nth-child(odd/even)` → 次回：テーブルの縞々に使う予定

### 次回の続き
- `:nth-child(odd/even)` で行の縞々
- 自動更新・更新日時の表示

---

## Day 23 — prices.html 完成（縞々・更新日時・自動更新）

### やったこと

**1. `:nth-child` で縞々（自力で記述）**
- `tbody tr:nth-child(odd) { background-color: white; }`
- `tbody tr:nth-child(even) { background-color: #f9f9f9; }`
- ついでにボタンの背景色もオレンジに統一（`th` と色を揃えた意図的な設計）

**2. 更新日時の表示**
- `<span id="last-updated">` を HTML に追加
- `const lastUpdated = document.getElementById('last-updated')` で取得
- `lastUpdated.textContent = '最終更新：' + new Date().toLocaleTimeString('ja-JP')` で表示

**3. 自動更新（`setInterval`）**
- `setInterval(loadPrices, 30000)` で30秒ごとに自動fetch

### 詰まったポイントと解決
- 変数名にハイフンは使えない（`last-updated` → `lastUpdated`）。JSはハイフンを引き算と解釈する
- `lastUpdated.textContent` をボタンのクリックハンドラに書いていた → `loadPrices()` の中に移動することで、ボタンでも自動更新でも両方に対応

### 学んだこと
- `:nth-child(odd/even)` で奇数・偶数行に別スタイルを当てられる
- `setInterval(関数, ミリ秒)` で定期実行できる
- 「どこから呼ばれても動く処理」は関数の中にまとめる（重複を避ける設計）
- `new Date().toLocaleTimeString('ja-JP')` で現在時刻を日本語形式で取得

### prices.html 完成 ✓
テーブル表示・色分け・アニメーション・縞々・更新日時・自動更新 すべて実装完了

---

## Day 24 — 方針転換・マイルストーン設定

### やったこと
- プロジェクトの目標を整理した
- 学習スタイルを「AIがリード」→「ユーザーがリード、AIが実装」に切り替えた
- schedule.htmlを更新（2026年に修正・Level C完了タスクを反映・今後の予定追加）

### 決めたこと
**プロジェクトの目標（2つの柱）**
- 自分でHTML/CSS/JSをある程度書けるようになる
- AIに適切な指示ができるようになる

**実際の使い方イメージ**
- テンプレートを探して加工する → コードを読んで理解
- AIに作らせる → 意図を伝えて・出力を確認

**マイルストーン（期限なし・達成事実で測る）**
- M1：知らないHTMLを読んで、構造を説明できる
- M2：テンプレートのCSSを自分好みに変更できる
- M3：作りたい機能をAIに説明して、意図通りに実装してもらえる
- M4：AIが書いたコードを読んで、何をしているか説明できる

### 次回の続き
- prices.html にグラフを追加する（M3の練習）
- まず「どんなグラフを・どこに・どうやって」を自分で言語化することから始める

---

## Day 25 — グラフ実装（M3・M4の練習）

### やったこと

**1. M3 練習：仕様の言語化**
- 「グラフを表示する」だけでは曖昧 → AIに伝わる粒度まで詰めた
- 最終仕様：折れ線グラフ（BTC直近3ヶ月）+ 円グラフ（4通貨の時価総額シェア）
- ライブラリなし（HTML/CSS/SVGで自作）
- レイアウト：PC=横並び、スマホ=縦並び（Grid + メディアクエリ）

**2. 円グラフの実装（AIが実装、ユーザーが指示）**
- API URLに `include_market_cap=true` を追加して時価総額を取得
- `COINS` 配列をトップレベルに抽出（loadPricesとdrawPieChartで共有するため）
- SVGの `<path>` 要素で4つのスライスを描画

**3. M4 練習：コードを読んで説明**
- `drawPieChart` の全体像：データから円グラフを描く関数 ✓
- `COINS.reduce((sum, c) => sum + data[c.id].jpy_market_cap, 0)` → 4通貨の時価総額の合計
- `cx + r * Math.cos((angle - 90) * Math.PI / 180)` → 角度から円周上の(x,y)座標を計算
- SVGパス命令：M（移動）→ L（直線）→ A（円弧）→ Z（閉じる）
  - 1スライス＝中心から円周(x1,y1)へ直線、円周を弧で(x2,y2)まで、中心へ戻る

### 学んだこと
- JSの計算記号：`*` がかけ算、`/` が割り算
- SVGの `<path>` の `d` 属性は描画コマンドの羅列
- パスコマンドの意味：
  - `M`（Move）：ペンを指定座標に移動する。線は引かない
  - `L`（Line）：現在位置から指定座標まで直線を引く
  - `A`（Arc）：現在位置から指定座標まで円弧を引く（半径・向き等を引数で指定）
  - `Z`（Close）：始点まで自動で線を引いて閉じる
- パイの一切れは「直線2本＋円弧1本」で作る（M中心 → L円周start → A円弧end → Z）

### 詰まったポイント
- `total` を最初「割合の合計」と誤解 → 実際は「時価総額の合計」（割合の分母になる）
- `x1` を最初「銘柄のシェア」と誤解 → 実際は「円周上の座標」

### 次回の続き
- `largeArc` フラグの意味（180度を超えるかで0/1になる）
- 折れ線グラフの実装（過去3ヶ月のBTC価格）

---

## Day 26 — largeArcフラグの理解（短時間セッション）

### 学んだこと
- 円の上に2点を置いたとき、それらを結ぶ円弧は2通りある（短い弧と長い弧）
- ちょうど180°のときだけ両方の長さが等しい
- SVGの `A`（Arc）コマンドはどっちを描くかを `largeArc` フラグ（0 or 1）で指定する
  - `0` = 短い弧
  - `1` = 長い弧
- prices.html の円グラフでは BTC のスライスだけが180°を超えるので `largeArc = 1`

### コード
```js
const largeArc = (endAngle - startAngle) > 180 ? 1 : 0;
```

### 次回の続き
- 折れ線グラフの実装（過去3ヶ月のBTC価格）

---

## Day 27 — 折れ線グラフ実装・関数定義と呼び出し

### やったこと
- 折れ線グラフの仕様確定：縦軸の値ラベルと横軸の月ラベルだけ（グリッド線・データ点なし）
- AIに実装を依頼（M3練習）→ `drawLineChart()` 関数を追加
- API：`/coins/bitcoin/market_chart?vs_currency=jpy&days=90`（過去90日分の価格）
- ページ読み込み時に1回呼ぶ（過去データなので30秒ごとに更新する必要なし）

### 学んだこと：関数の「定義」と「呼び出し」
- 関数定義（`function drawLineChart() {...}`）は「レシピを書く」行為。書いただけでは何も起こらない
- 関数呼び出し（`drawLineChart();`）は「レシピを実行する」行為。これが書かれて初めて処理が走る
- JavaScriptには「巻き上げ（hoisting）」という仕組みがあり、呼び出しが定義より前の行にあっても動作する

### 次回の続き
- `drawLineChart` のコードを読む（M4練習）

---

## Day 28 — drawLineChartのコードを読む（M4練習・前半）

### 関数の構造を8ブロックに分解
- ① データ取得（fetch）
- ② SVG全体のサイズ・余白の定義
- ③ 価格の最小・最大を求める
- ④ 各価格点をSVG座標に変換
- ⑤ 線（path）を組み立てる
- ⑥ Y軸ラベルを作る
- ⑦ X軸ラベル（月の境目）を作る
- ⑧ SVGに全部書き込む

### 今日読んだのは ③④⑤

**③ 最小・最大の取得**
- `prices.map(p => p[1])` で各ペア `[日付, 価格]` から価格だけ取り出す
- `Math.min(...)` `Math.max(...)` に数値配列を渡して最小・最大を取る
- `range = maxP - minP || 1` の `|| 1` はゼロ割り防止の保険

**④ 座標変換（核心）**
- x = 「初日からの日数」を「チャート幅における位置」に変換
- y = 「最低価格からの差」を「チャート高さにおける位置」に変換
- 大事な気づき：**SVGの座標系は左上が原点**。紙にグラフを描く感覚（左下原点）とは逆
- だから価格を縦軸位置に変換するときは `1 - ...` でひっくり返す必要がある

**⑤ 線の組み立て**
- 90個の点を `M ... L ... L ... L ...` という文字列にする
- 最初の点だけ `M`（移動）、残りは `L`（直線）
- 三項演算子 `i === 0 ? 'M' : 'L'` で切り替え（色分けで使ったパターンと同じ）

### 次回の続き
- ⑥ Y軸ラベル（formatPrice関数で「¥1,500万」形式に整形する処理）
- ⑦ X軸ラベル（月の境目を見つけて「3月」「4月」「5月」を配置する処理）

---

## Day 29 — drawLineChartのコードを読む（M4練習・後半）

### 今日読んだのは ⑥⑦

**⑥ Y軸ラベル**
- `formatPrice = v => '¥' + ...` は **アロー関数の定義**。`function(v) { return ... }` と同じ意味の短縮形
- `[maxP, (minP+maxP)/2, minP]` という配列を作って `.map()` で3つのラベル要素を生成
- 配列の順番が「最高値→中間値→最低値」なのは、SVGの y軸が「上=0、下=大」だから（i=0が一番上、i=2が一番下）
- `<text>` の `text-anchor="end"` は右揃え（テキストの終端を x の位置に合わせる）

**⑦ X軸ラベル — 3パート構成**

A. 最初と最後の日付を取り出す
- `prices[0][0]` = データ最初の日付、`prices[prices.length-1][0]` = 最後の日付
- `new Date(タイムスタンプ)` で数値→Dateオブジェクト

B. 月の境目（毎月1日）のリストを作る
- `new Date(年, 月+1, 1)` で「次の月の1日」のDateを作る
- `while (cur <= lastDate)` ループで毎月1日を集める：
  - データ開始が 2/15 なら、3/1 から始めて 4/1, 5/1 と進む
  - 「データ範囲に収まる月初め」だけが months に入る
- データ開始が月の途中なら、その月のラベルは出さない（例：2月は飛ばす）

C. 座標計算と表示
- ratio = `(月1日の時刻 - データ開始時刻) / 全期間` → 0〜1の比率
- x = `padL + ratio * chartW` → ④と同じ「比率×幅+余白」のパターン
- y = `h - padB + 15` → チャートの下端より15px下にラベル
- `text-anchor="middle"` で x位置を中心にテキスト配置（Y軸ラベルは end だった）
- `m.getMonth() + 1` で表示用に「1〜12月」にする（getMonth() は 0〜11）

### 学んだこと
- アロー関数の書き方（`v => ...`）
- `Date` オブジェクトの作り方と取り出し方（`getFullYear()`, `getMonth()`, `getTime()`）
- `getMonth()` は0始まりという罠
- `while` ループの基本構造
- `text-anchor` の値（end / middle / start）

### prices.html について
これでM3（仕様の言語化）とM4（コードを読んで説明）の両方を `drawLineChart` で経験できた。
時価総額シェアの円グラフと、ビットコイン価格推移の折れ線グラフを完成させ、両方とも内部のコードを理解した状態。

### 次回の続き
- 何をするかは未定。マイルストーンの観点で見ると、prices.html での M1〜M4 練習は一段落
- 次は別のテーマ／ページでマイルストーン練習を継続するか、prices.html をさらに磨くか

---

## Day 30 — 次の方向決め（M1・M2へ）

### 決めたこと
- 次に取り組むのは M1（知らないHTMLを読んで構造を説明できる）と M2（テンプレートのCSSを変更できる）
- M1・M2 が達成できれば、プロジェクトの目標はいったん達成
- フリーテンプレート配布サイトとして **HTML5 UP**（html5up.net）を使う
- テンプレートは **Phantom**（一番素直な構造のもの）を選択

### 進め方のイメージ
1. html5up.net/phantom からzipをダウンロード
2. `/mnt/data/web-learning/phantom/` に解凍配置
3. フォルダ構造を確認 → index.htmlを上から読み解く
4. 一通り構造を理解したら、CSSを自分好みに変更（M2練習）

### 進捗マップ
| 目標 | マイルストーン | 状況 |
|---|---|---|
| 自分でHTML/CSS/JSをある程度書ける | M1, M2 | 次回着手 |
| AIに適切な指示ができる | M3, M4 | ✓ 達成（prices.htmlで練習済み） |

### 次回の続き
- Phantomのダウンロード・配置の確認
- フォルダ構造の確認とindex.htmlの読み解き開始

---

## Day 31 — Phantom読解開始（M1練習・前半）

### やったこと
- Phantomを `/mnt/data/web-learning/template/html5up-phantom/` に配置済みを確認
- フォルダ構造を確認
- index.htmlの大枠を把握
- Header（19-36行）と Menu（39-48行）の中身を読解

### フォルダ構造の理解
```
html5up-phantom/
├── index.html        メインページ
├── elements.html     UI部品のサンプル集（読まなくていい）
├── generic.html      汎用ページ用テンプレート
├── assets/
│   ├── css/          スタイルシート
│   ├── js/           JavaScript
│   ├── sass/         SASSソース（CSSの拡張版・触らなくていい）
│   └── webfonts/     Webフォント
└── images/           画像
```

### index.html の4ブロック構造（HTMLコメントが目印）
| 行 | ブロック | 役割 |
|---|---|---|
| 19 | Header | 上部の固定バー（ロゴ＋Menuリンク） |
| 39 | Menu | 隠れナビゲーション（クリックで横からスライド） |
| 50 | Main | 大きな見出し＋12個のタイル |
| 195 | Footer | 問い合わせフォーム＋SNSアイコン |

### 学んだこと
**1. M1の鉄則：コードを読む前にブラウザで実物を見る**
- 「あの見た目はこの部分か」と紐付けながら読める

**2. テンプレート特有の慣習**
- ラテン語のダミーテキスト（Lorem Ipsum）が使われている
- 複数のリンクが同じファイル（generic.html）に飛ぶ → カスタマイズで差し替える前提
- 「とりあえず動く状態」を提供して、利用者が中身を埋める

**3. リンクの3パターン（自分の気づき）**
| パターン | 例 | 動作 |
|---|---|---|
| ① 別ファイル | `href="generic.html"` | 別ページに移動 |
| ② 同じページ内アンカー | `href="#section"` | id="section"までスクロール |
| ③ ② + JSで動き追加 | `href="#menu"` | 本来②だがJSがスライドイン等の動作に変換 |

### 次回の続き
- Main（50-193行）の読解：ヘッダー部分とtileセクション
- Footer（195-234行）の読解

---

## Day 32 — Phantom読解：Mainブロック（M1練習・後半）

### やったこと
- Main（50-193行）の入れ子構造を把握
- 1タイル（article）の中身を読解
- `style1`〜`style6` の意味を理解
- 閉じタグの順番ルールを確認

### 学んだこと

**1. Mainの入れ子構造**
```
<div id="main">
  <div class="inner">
    <header>           大見出し＋説明文
    <section class="tiles">
      <article class="style1">...</article> × 12個
```

**2. 1タイル（article）の構造と意図**
- `<span class="image">` と `<a>` を分ける理由：それぞれ独立してCSSでスタイル付けするため
  - 画像は常に表示
  - `<a>` がオーバーレイになり、ホバーで本文が出てくる・タイルが浮き上がる
- `<a>` が `<h2>` と `<p>` を両方包む理由：タイル全体（文字部分）を1つのクリック可能領域にするため

**3. `style1`〜`style6` の意味**
- 6色のバリエーションを表すクラス。HTMLには色の指定はなく、`assets/css/main.css` 側で `.tiles article.style1 { ... }` のように色を当てている
- HTML（構造）とCSS（見た目）の分離原則そのもの
- M2の布石：このクラス名を頼りにCSSを探せば色を変えられる

**4. 閉じタグの順番ルール**
- 開く順（外→内）と閉じる順（内→外）は鏡写し
- インデントの深さ＝入れ子の深さ。M1で構造を読むときの大事な手がかり

### 観察で確認したこと（ブラウザ実機）
- タイルにマウスを乗せると：画像はずっと見える、タイルが浮き上がる、本文が出てくる
- 同じstyle番号のタイル同士は同じ色

### 次回の続き
- Footer（195-234行）の読解：問い合わせフォーム＋SNSアイコン
- その後、CSSを開いて `.tiles article.style1` を探す（M1完了→M2着手）

---

## Day 33 — Phantom読解：Footerブロック（M1練習・完了）

### やったこと
- Footer（195-234行）を3パートに分けて読解
- パート1：問い合わせフォーム
- パート2：SNSアイコン（Font Awesome）
- パート3：コピーライト

### 学んだこと

**1. フォームの復習＋新要素**
- `class="field half"` の `half` → 1行の半分の幅。NameとEmailを横並びにするための指定（CSSは別途）
- `<input type="submit" value="Send">` と `<button type="submit">Send</button>` は機能的に同じ。書き方の流儀違い
  - input：表示テキストは `value` 属性
  - button：表示テキストはタグの中身（HTMLも書ける、現在の主流）

**2. SNSアイコンのクラス構成（`icon brands style2 fa-twitter`）**
| クラス | 役割 |
|---|---|
| `icon` | Phantom独自のアイコン整形 |
| `brands` | Font Awesome の分類（ブランドロゴ系） |
| `style2` | Phantom独自のスタイルバリエーション |
| `fa-twitter` | Font Awesome の個別アイコン指定 |

- `fa` = Font Awesome（アイコンフォントのライブラリ）
- ブランド系は `brands`、塗りつぶし系は `solid`（電話・メールなど）

**3. アクセシビリティ（a11y）**
- `<span class="label">Twitter</span>` は画面では見えないがスクリーンリーダーには読まれる
- CSSで「見た目だけ消す」テクニック（`position:absolute; width:1px; height:1px; overflow:hidden;` など）
- Day 2の「`<thead>` を分ける理由」と同じ思想：見た目のためでなく意味のために書く

**4. HTMLエンティティ**
- `&copy;` → ©
- `&lt;` → <、`&gt;` → >、`&amp;` → &、`&nbsp;` → 空白
- HTMLで特別な意味を持つ記号を表示するための書き方

### M1達成
- Phantom全体（Header → Menu → Main → Footer）の構造を読み解いた
- 知らないテンプレートでも、コメントとインデントを頼りに構造を把握できることを確認

### 次回の続き
- M2着手：CSS（main.css）の全体構造をまず把握する（どこに何が書かれているかの地図作り）
- その後、`.tiles article.style1` などを探してタイルの色を変えてみる

---

## Day 34 — M2着手：main.css の構造把握とテンプレート編集

### やったこと
- main.css（3328行）の全体構造を地図化
- `/* Tiles */` セクション（2564〜2828行）の内部構造を把握
- 自力で3パターンの編集を実行・ブラウザで確認

### 学んだこと

**1. main.css の全体構造**
- 上から：`@import` → リセットCSS → Basic → Type → Row（約1400行、レスポンシブ調整が大量）→ 各UI部品（Icon/List/Form/Table/Button等）→ Tiles → Header → Menu → Main → Footer → Wrapper
- 後半の並び順が index.html の構造とほぼ対応している
- 最初の約60行はリセットCSS（要素名を大量に並べて margin/padding を 0 に）— ブラウザ差を消してから自分で設計し直す定番手法

**2. `/* Tiles */` セクションの設計パターン**
- 並び順：通常状態（外側→内側）→ バリエーション（.style1〜6）→ 特殊状態（:hover, is-preload, is-touch）→ レスポンシブ（@media）
- この「外→内→状態→画面幅」の順はほぼ全セクション共通
- インデントの深さ＝詳細度（入れ子の深さ）と対応している

**3. タイルの色の仕組み（疑似要素 :before）**
- `<span class="image">` の中身は `<img>` だけ
- CSSの `:before`（疑似要素）で「画像の手前に半透明の色レイヤー」を生成（`content: ''` + `position: absolute` + `opacity: 0.8`）
- そのレイヤーの色が `.style1`〜`.style6` で切り替わる → 写真が透けて「色みがかった写真」になる
- ホバー時は `:hover > .image:before` でレイヤーを #333 に・濃くしている（写真も1.1倍に拡大）

**4. M2の3パターン（自力で実施）**
| パターン | やったこと |
|---|---|
| 既存の色を変える | `.tiles article.style1 > .image:before` の `background-color` を #f2849e → #381f24 |
| 既存の値を整合させて変える | タイルの `border-radius: 4px` を 34px に（写真枠とオーバーレイの2か所を同じ値に。ズレ防止） |
| 無いプロパティを書き足す | `#header` に `background-color: #8499e7;` を追加（元々背景色の指定が無かった） |

- 「変えたい見た目 → 該当CSSを探す。無ければ書き足す」が一通りできた
- 角丸が2か所にある理由：写真の枠（`.tiles article > .image`）と文字オーバーレイ（`.tiles article > a`）がぴったり重なっているため、片方だけ変えると角がズレる

### M2 ほぼ達成
- テンプレートのCSSを読んで、変えたい箇所を自分で見つけ、編集し、ブラウザで確認できた

### 次回の続き
- M2の練習を広げる（残りのタイル色 style2〜6、見出しのフォント・色、ホバー時の色など）か、M1/M2の総仕上げ
- プロジェクト目標（自分で書ける ＋ AIに指示できる）の達成状況を振り返る

---

## Day 35 — プロジェクトの振り返り・次の方向決め

### やったこと
- Day 1〜34 を振り返り、2つの目標とマイルストーンM1〜M4の達成状況を確認
- 次の学習テーマを決定

### 振り返りの結論
**2つの目標：両方とも達成**
- ① 自分でHTML/CSS/JSをある程度書ける → profile.html / contact.html / prices.html を制作。HTML構造・CSS（Grid/Flex/トランジション/アニメ/疑似要素）・JS（DOM/イベント/fetch/async-await/配列/SVG）を理解
- ② AIに適切な指示ができる → M3（仕様の言語化）・M4（AIのコード読解）を prices.html のグラフで実践

**マイルストーン：M1〜M4 すべて達成**
- M1：Phantomテンプレート全体を読解
- M2：main.css を読み、色・角丸・背景色を自力編集
- M3：グラフの仕様をAIに伝えて実装してもらった
- M4：drawLineChart / drawPieChart を読んで説明できた

### 本人の自己評価
- 一番の手応え：始めた頃に比べ「全体の構造と関係をイメージできる・理解できる」ようになったこと。今後は細部はAIに指示しながら作れそう
- 残る不安：グラフをライブラリなしで自作したため、**ライブラリの使い方がイメージできない**

### 次のテーマ決定：ライブラリを使ってみる（Chart.js）
理由：prices.html のグラフを「SVGで手作り」した経験があるので、同じものを「ライブラリで作り直す」と手作り vs ライブラリを直接比較でき、ライブラリが何を肩代わりするか実感できる。

進め方のイメージ：
1. Chart.js の CDN を1行追加（ライブラリの読み込み方を学ぶ）
2. 公式の最小サンプルで折れ線グラフを表示
3. prices.html の実データ（CoinGecko 90日分）につなぐ
4. 自作 drawLineChart と並べて比較・言語化（M4の応用）
5. 円グラフも同様
6. 余裕があれば自作版を Chart.js 版に置き換え

学べること：ライブラリの読み込み方／「設定オブジェクトを渡す」という使い方のパターン／公式ドキュメントの読み方（AIへの指示にも直結）／自作 vs ライブラリのトレードオフ

### 次回の続き
- Chart.js 着手：CDN追加 → 最小サンプル表示
- ※ Chart.js が一段落したら、次の方向はまた相談して決める（ユーザー希望）

---

## Day 36 — Chart.js 入門：最小サンプルと設定オブジェクトの体感

### やったこと
- chartjs-test.html を新規作成（24行）
- CDN 1行で Chart.js を読み込み、`new Chart(ctx, { type, data })` で折れ線グラフが表示できることを確認
- 設定オブジェクトをいじる練習（①〜④）
  - ① `type` を 'line' → 'bar' / 'pie' / 'doughnut' などに変更
  - ② `borderColor` / `backgroundColor` を dataset に追加
  - ③ `options.plugins.title` でタイトル表示
  - ④ `datasets` 配列に2つ目を追加（売上＋経費の2本線）

### 学んだこと

**1. ライブラリとは「他人が書いた便利なJSコード」**
- 使う側のやることは2つだけ：①読み込む（`<script src=CDN>`）②呼び出す（`new Chart(...)`）
- グラフ描画の全工程（座標変換・軸ラベル配置・線の組み立て・色付け・ツールチップ）をライブラリが肩代わり
- 自作 drawLineChart 約50行 → Chart.js 7行で同等のグラフ

**2. Chart.js の「型」**
- `new Chart(canvas要素, 設定オブジェクト)` の2引数
- 設定オブジェクトは `type` / `data` / `options` の3階層
- `data` には `labels` と `datasets`（配列。複数の系列を持てる）
- `options.plugins.title` などで装飾を追加

**3. 設定オブジェクトを触って分かったこと**
- `type` を変えるだけで同じデータが棒・円・ドーナツに切り替わる → グラフ種類は「設定値」
- 線の色などはライブラリが自動で決めるが、書き加えれば上書きできる
- マウスホバーでツールチップが自動で出る・凡例も自動表示 — ライブラリが面倒を見てくれる範囲が広い

**4. 括弧の対応でハマった（重要な教訓）**
- ④で2つ目の dataset を追加するとき、`borderColor` 等を入れる位置を間違えた
- さらに `options` を `data` の中に書いてしまい、最外の `});` も不足
- JSの設定オブジェクトでよくあるトラブルパターン
- 原則：
  - `{ }` の中にしかプロパティは書けない
  - 入れ子は鏡写しの順で閉じる
  - インデントが崩れていたら構造が壊れているサイン
  - 括弧1つにつき「開く相手」が必ず1つある

### 次回の続き
- 実データ接続：CoinGecko API（90日分のBTC価格）を fetch → Chart.js に渡す
- 自作 drawLineChart と Chart.js 版を並べて比較・言語化（M4の応用）
- その後：円グラフも同様に → 余裕あれば自作版を置き換え

---

## Day 37 — Chart.js に CoinGecko 実データを接続・比較

### やったこと
- chartjs-test.html を書き換え、CoinGecko の BTC 90日価格データを fetch
- ペアの配列 `[[ts, price], ...]` を `labels`（日付文字列）と `data`（価格）の2配列に分解
- `async function drawLineChart()` で囲み、関数末尾で `drawLineChart()` を呼ぶ構成
- 自作 drawLineChart（prices.html）と並べて比較

### 学んだこと

**1. fetch → Chart.js の組み合わせパターン**
- 関数を `async` で囲む → `await fetch` でデータ取得
- 取得したデータの形を Chart.js が要求する形（labels + data の2配列）に**変換**するのが要
- 配列の分解：`prices.map(p => p[0])` でタイムスタンプ、`prices.map(p => p[1])` で価格

**2. 比較で見えたこと（Chart.js が肩代わりするもの）**
- ツールチップ（線にマウスを乗せると値が表示）
- 凡例の自動表示
- グリッド線・目盛り線
- 軸ラベルの自動配置
- 色の自動決定
- 自作 drawLineChart は約100行、Chart.js 版は約20行（うち fetch とデータ変換が大半。Chart.js への指示はわずか）

**3. ハマったポイント**
- `prices.map(p => p[1])` の `p` を書き忘れて `p => [1]` になり、`[1]` の配列ばかりになった
- 関数名の大文字小文字が一致せず `drawchart()` で呼んで `is not defined` エラー
- JS は大文字小文字を厳密に区別する
- 開発者ツール → Console を見る習慣をつける（エラー行・原因が分かる）

**4. 本人の自己評価**
- 「Chart.jsの方がだいぶ便利。ただ、どちらもいまひとつ消化しきれていない」
- → これは「分かる」と「使える」の段差で、能力不足ではない。**勉強し直すより、似たことをもう一度やる**のが効く

### 次回の続き
- 円グラフ（時価総額シェア）を Chart.js で作り直す（type:'pie' に変える応用＝同じ型の繰り返しで定着、データ変換は折れ線とは少し違う）
- 余裕あれば自作版を Chart.js 版に置き換えて prices.html を整理

---

## Day 38 — Chart.js 円グラフ・JS学習の方向転換

### やったこと
- chartjs-test.html に2つ目のグラフ（円グラフ）を追加
- CoinGecko の `/simple/price`（時価総額込み）から4通貨の時価総額を取得
- `type: 'pie'` で円グラフを描画。生の時価総額4つを渡すだけで割合・角度・座標は Chart.js が自動計算
- 折れ線・円グラフの両方を実データ・Chart.js 版で完成

### 学んだこと
- Chart.js は同じ型（`new Chart(ctx, { type, data })`）を使い回せる。違うのは fetch の URL とデータ変換だけ
- 自作 drawPieChart の三角関数・largeArc・SVGパス生成（Day 25-26 で苦労した部分）が丸ごと不要になった

### 重要：JS学習の方向転換（本人の自己診断から）
本人の気づき：「Chart.js が消化できないのではなく、もっと手前で詰まっている。HTML/CSS は書かれたコードを見れば何をしようとしているか掴めるのに、JS はそれが掴めない・腹落ちしない。真似してタイプしても、なぜそのコードなのか・なぜそうなるのか自分で説明できない」

原因の整理：
- HTML/CSS は「宣言型」＝結果そのものを書く。コードと結果がほぼ1対1。静止画のように眺めれば分かる
- JS は「手続き型」＝結果ではなく手順（命令の列）を書く。変数の値は時間とともに変わる。頭の中で1行ずつ実行して値の変化を追わないと分からない（動画）
- これまでの JS 学習はいきなり実戦コード（fetch/async/map/SVG/Chart.js）から入った。HTML/CSS では宣言型なのでそれで通用したが、JS は「コードを1行ずつ実行して値を追う」土台を飛ばしたまま応用に来てしまった
- 「動くけど腹落ちしない」のは能力ではなく順番の問題

### 決めたこと：JSの土台を「実行を追う」練習で作り直す
- Chart.js はいったん中断
- ごく小さなコード（3〜8行）を、実行前に「各行のあと変数に何が入っているか」を予想 → `console.log`／デバッガで確認
- 本人が得意な「観察→予想→確認」スタイルで進める。`console.log` とデバッガ（1行ずつ止めて見る機能）が「見えない実行」を可視化する道具（HTML/CSSでの Elements タブに相当）
- 積み直す順番：変数 → 順次実行 → 条件分岐 → ループ → 関数（定義と呼び出し）→ 配列・オブジェクト
- 後退ではない。prices.html や Chart.js を作った経験があるので、土台ができると過去のコードに後から線がつながる

### 次回の続き
- JSの土台練習スタート：変数と順次実行から。小さなコードの「実行を追う」予想→確認
- まず `console.log` で値を見る習慣・開発者ツールの Console / デバッガの使い方も導入

---

## Day 39 — JS土台づくり①：変数・順次実行・代入

### やったこと
- js-practice.html を新規作成（JS実行を追うための練習用ファイル）
- 開発者ツールの Console タブの使い方を導入
- 小さなコード（3〜8行）を「実行前に予想 → Consoleで確認」する練習を4本

### 学んだこと

**1. JSの大原則**
- コードは上から下へ、1行ずつ、順番に実行される
- 変数は「値を入れる箱」。中身は後から入れ替えられる
- 同じ変数でも、実行のタイミングが違えば中身が違う（JSは「動画」、HTML/CSSは「静止画」）

**2. `console.log` の正体**
- 「変数を映し続ける窓」ではなく「その瞬間の値を1行 *書き残す*」命令（スナップショット／記録）
- 一度表示された行は変わらない。`console.log` が呼ばれるたびに新しい行が増える
- 「値が変わること」と「値が表示されること」は別。`console.log` が無ければ画面に出ない＝JSの実行は基本目に見えない。`console.log` は見えない実行に覗き穴を開ける道具
- 出力の行頭 `js-practice.html:13` は「ファイル名:行番号」。どの行から出たログか分かる

**3. ドット記法（`console.log` の構造）**
- `console`（もの＝対象）と `.log`（その動作のひとつ）は別物。ドットは「〜の」（所属）
- `console` は他に `.error`（赤）`.warn`（黄）も持つ
- 「もの . 動作()」は既出パターン（`document.getElementById` `prices.map` `res.json` `classList.add` も同じ形）

**4. 警告 vs エラー**
- エラー：続行不可能。実行が止まる。赤。（例：Day37の `drawchart is not defined` → グラフが出なかった）
- 警告：続行はできる。実行は止まらない。黄。（例：`file://` の Unsafe attempt 警告 → 10,20 はちゃんと表示された）
- ログ：`console.log` で頼んだ情報。普通色
- 判断：期待した動きが起きていない＋赤＝エラー（直す）。動いている＋黄＝警告（多くは無視可）
- file:// で開くと出る警告は今のJS練習に影響なし。無視してよい

**5. `=` は「代入」（最重要）**
- 数学の「等しい」ではない。命令：「右側の値を、左側の箱に入れろ」（右→左）
- だから `x = x + 1` も成立（今のxに1足した値をxに入れ直す）
- `a = b` は b の*値*を a にコピーするだけ。コピー後、a と b は別の独立した箱
- 誤解しやすい点：`a = b` のあと b を変えても a は変わらない（つながっていない）
  - 練習4で本人は「つながる（X）」と予想 → 実際は 3,3,3,**3**,100 ではなく 3,3,**3**,100。a は 3 のまま
  - 例え：`a = b` は b の紙をコピーして a のフォルダに入れる。原本を書き換えても複製は変わらない

**6. セミコロン `;`（ざっくり）**
- 1つの命令（文）の終わりの印。文章の「。」と同じ
- 書き忘れても自動補完（ASI）でたいてい動くが、まれにバグるので必ず書くのが推奨
- ブロックの閉じ `}` 単体（function / if / for の `}`）には付けない。`new Chart(...);` のような命令には付ける

### 次回の続き
- JS土台づくり②：計算・式（`x + 1` などの右側の評価）、または条件分岐（if）へ
- 引き続き「予想 → console.logで確認」スタイル。js-practice.html を使う

---

## Day 40 — JS土台づくり②：計算・式・「同じ箱／別の箱」

### やったこと
- js-practice.html で「予想 → Consoleで確認」の練習を2本（練習5・6）
- 式（計算）と、変数の値が持ち越される仕組みを確認

### 学んだこと

**1. 式（しき / expression）と `=` の右側**
- `=` の右側には計算が書ける。JSは右側を先に計算して1つの値にしてから、左の箱に入れる
- 演算子：`+`（足す）`-`（引く）`*`（かける）`/`（割る）
- `x = x + 1` が成り立つ理由：`=` は「等しい」ではなく命令、しかも右が先。「今の x に 1 足した値で x を上書き」
- 右側の変数（`count + 1` の `count` など）は、いつも「今この瞬間の箱の中身」を読む

**2. 「同じ箱／別の箱」の混乱を解消（重要）**
- 本人の混乱：「昨日（練習4）は値がリンクしなかったのに、今日（練習5）は count が 1→2→6 と持ち越される。矛盾では？」
- 解消：矛盾ではない。ルールは1つだけ
  - **どの箱も、最後に入れた値をずっと保つ。`箱 = 値` でその箱が左に来たときだけ中身が変わる**
- 練習4の `a`：`a = b` で 3 が入ったあと二度と `=` の左に来ない → 3 のまま保たれた（値は持ち越されていた）
- 練習5の `count`：毎行 `=` の左に来る → 毎回更新される
- 「別の箱どうし（a と b）は独立」と「同じ箱（count）は値を持ち越して更新」は、同じ1つのルールの当てはめ結果の違い

**3. 練習でのミスと気づき**
- 練習5：`count + 1` を毎回 0+1 と計算してしまった（count を初期値 0 にリセットしていた）→ 実際は前の行の値を持ち越す（0→1→2→6）
- 練習6：`q = p - 1` を `q = q - 1` と読み違えた（10-1=9 と予想、正解は 12-1=11）。本人が自分でミスに気づいた
- 教訓：`=` の右側は「どの箱を読むか」を正確に指定している。コードは流し読みせず1文字ずつ正確に読む

### 次回の続き
- JS土台づくり③：条件分岐 `if`
- 引き続き「予想 → console.logで確認」スタイル、js-practice.html を使う

---

## Day 41 — JS土台づくり③：条件分岐（if / else / else if）

### やったこと
- js-practice.html で「予想 → Consoleで確認」の練習を4本（練習7〜10）
- 真偽値・比較・if / else / else if を確認

### 学んだこと

**1. 新しい値：真偽値（boolean）**
- 数字以外の値の種類。`true`（はい）/ `false`（いいえ）の2つだけ
- 比較すると真偽値が出る。`5 > 3` という式は `true` という値になる（`3 + 2` が `5` になるのと同じ「式」）
- 比較演算子：`>` `<` `>=` `<=` `===`（等しい）`!==`（等しくない）

**2. `=` と `===` の違い（重要）**
- `=`（1つ）＝代入：右の値を左の箱に入れる命令
- `===`（3つ）＝比較：左と右が等しいか調べて true/false を出す
- 「等しいか調べたい」ときは必ず `===`

**3. `if`**
- `if (条件) { ブロック }`：条件（真偽値になる式）を計算 → true なら `{ }` を実行、false ならまるごとスキップ
- `if` がスキップするのは自分の `{ }` の中だけ。その先のコード（if の外）は普通に実行される
- ブロックの閉じ `}` には `;` を付けない

**4. `else` / `else if`**
- `else`：条件が false のときの行き先。if と else はどちらか片方だけ必ず実行される
- `else if`：3択以上に分岐。条件を上から順にチェックし、**最初に true になったブロックだけ実行、残りは全部スキップ**
- 実行されるブロックは必ず1つだけ
- `if` の本質：同じコードでも、条件に使う値しだいで通る道（branch）が変わる。「そのときの値を見て行き先を選ぶ」

**5. 読み取り精度の課題（習慣の話）**
- 練習6で `p`→`q`、練習10で `18`→`10` と読み違いが2回。いずれも自分で気づいて訂正できた
- 実行を追う力は付いている。課題は読み取りの精度のほう。条件や式は「指で押さえてゆっくり」読む

### 次回の続き
- JS土台づくり③続き：`else if` の「順番のワナ」（条件の並び順を誤ると永遠に実行されないブロックができる）
- その後：ループ（くりかえし）へ
- 引き続き「予想 → console.logで確認」スタイル、js-practice.html を使う

---

## Day 42 — JS土台づくり④：else if の順番のワナ・while ループ

### やったこと
- js-practice.html で練習11〜14を実施
- `else if` の順番のワナを実体験
- `while` ループの基本パターン（カウンタ・合計）

### 学んだこと

**1. `else if` の順番のワナ**
- `>= 60` を先に書き `>= 80` を後に書くと、score=90 でも **「合格」だけ**が出てしまう（`>= 60` が先に true になり「最初の true が勝ち」のルールで `>= 80` は届かない）
- 直し方：**狭い・厳しい条件を先、広い・緩い条件を後**。`>= 80` → `>= 60` → else の順で並べる
- コードのルールに合っていても意図に合っていなければバグ。else if を書くときの注意点

**2. `while` の動き方**
- `while (条件) { ... }`：条件チェック → true ならブロック実行 → また条件チェック … false で抜ける
- `if` との違い：if は1回で終わり、while は条件が false になるまで戻る
- ブロックの中で**条件に関係する変数を更新**しないと無限ループになる
- 練習13（i=0; while(i<3){...}）：i は 0→1→2→3 と変化、条件は true,true,true,false。ブロックは3回実行、出力は 0,1,2,終了

**3. 合計を求めるパターン（accumulator）**
- 練習14：sum=0 から始め、while で n=1〜5 を順に足していき sum=15
- 2つの箱（sum と n）が両方毎周変化する。両方追う必要がある
- prices.html の `COINS.reduce((sum, c) => ...)` も内部はこの考え方

**4. ミスのパターン（再発）**
- 練習14で `sum = sum + n` を `sum + 1` と計算してしまい sum: 0→1→2→3→4→5 と予想（実物は 15）
- 練習6（p/q）、練習10（18/10）に続き、**コードを正確に読む**習慣の問題で3回目
- 知識ではなく癖。「指で押さえてゆっくり」「右側の変数は今この瞬間の中身を読む」を意識
- 一方で、ループの構造（何周・いつ脱出）は完璧に追えた。仕組みの理解は安定している

### 本人のコメント
「くやしい」— ちょっと悔しい感覚。これは「できる人の感覚」。

### 次回の続き
- JS土台づくり⑤：while のもう1パターン（カウントダウン）または `for` ループへ
- 「読み取り精度」を意識しつつ進める
- その後、配列・関数へ進めて prices.html / Chart.js の理解にループから入って行く

---

## Day 43 — JS土台づくり⑤：for ループ・中間振り返り

### やったこと
- js-practice.html で練習15・16
- 練習15：while のカウントダウン（5→1→発射）。予想通り正解
- 練習16：for ループ。予想通り正解（10,20,30,40,done）
- JS土台づくり（Day 39〜43）の中間振り返りと今後の見通しを確認

### 学んだこと

**1. while のカウントダウン**
- 減るカウンタも増えるカウンタと仕組みは同じ。条件に使う変数をブロック内で変えるだけ
- 練習15：`i=5; while(i>0){ log(i); i=i-1; }` → 5,4,3,2,1,発射

**2. for ループ**
- `for (① 初期化; ② 条件; ③ 更新) { ブロック }`
- while の3つの部品（初期化＝ループ前・条件＝while()・更新＝ブロック末尾）を `for ( )` の1行に集めた形
- while と for はやっていることは同じ
- 実行順：① 初期化（最初1回）→ ② 条件チェック（falseで脱出）→ ③ ブロック → ④ 更新 → ②へ戻る
- `i++` は `i = i + 1` の短縮形

### 中間振り返り（JS土台づくり Day 39〜43）
全体を貫く軸＝「実行を1行ずつ追える」力をつけること。
- ① Day39：変数・順次実行・console.log・`=`は代入
- ② Day40：式・計算・`x=x+1`・同じ箱/別の箱
- ③ Day41：真偽値・比較・if/else/else if
- ④ Day42：else if順番のワナ・while
- ⑤ Day43：while カウントダウン・for

→ JSの「実行の文法」のコア（変数・順次実行・式・条件分岐・ループ）が一通り身についた。コードの骨組みを追える状態。
残る課題：式を1文字ずつ正確に読む習慣（読み違いが3回。知識でなく癖）。

### 今後の見通し（5/31ゴール「JS×AI」まで）
- ステップ1：土台の残り（配列 → オブジェクト → 関数 → 配列メソッド map 等）。あと数回
- ステップ2：実コードを読む。prices.html / chartjs-test.html の自分のJSを土台の力で読み直す＝基準①完成→②（AIのコード検証）へ
- ステップ3：作りたいJSの動きを手順・条件レベルで言語化してAIに指示＝基準③
- 現在地：「文法を覚える」段階の後半。次は配列から

### 次回の続き
- JS土台づくり⑥：配列（array）。複数の値をまとめて持つ。for ループと組み合わせる
- 引き続き「予想 → console.logで確認」スタイル、js-practice.html を使う

---

## Day 44 — JS土台づくり⑥：配列・読み取り手順の確立

### やったこと
- js-practice.html で練習17〜19
- 配列の基本、配列×ループ、読み取り精度の手順を練習

### 学んだこと

**1. 配列（array）の基本**
- `[ ]` で囲み `,` 区切り。複数の値を順番にまとめて1つの名前で持つ（`let nums = [10, 20, 30, 40]`）
- インデックス（番号）は **0 から**始まる。`nums[0]` が先頭、`nums[3]` が4番目（最後）
- `prices.html` の `p[1]` がインデックスアクセスだった（`p` は `[日付,価格]` の配列）
- `.length` は **個数**（4）。一番大きい番号（3）ではない。個数4・番号0〜3

**2. 配列 × ループ（最頻出パターン）**
- `for (let i = 0; i < nums.length; i++) { ... nums[i] ... }`
- `i` を 0 から、条件は `i < 配列.length`（`<=` でなく `<`）。i が 0〜3 とちょうど全番号をなぞる
- `i <= length` だと存在しない番号にアクセスしてエラーのもと
- `i`（インデックス＝位置の番号）と `nums[i]`（その位置の値）は別物

**3. 読み取り精度の手順を確立（重要な進展）**
- 練習18で `console.log(nums[i])` を `console.log(i)` と読み違え（読み違い4回目）
- 癖の正体：コードの「形」から予想している（「for ループ＝カウンタを出す」と決めつけ）。実際は出力行が文字通り何かが答え
- 対策テクニック：**予想の直前に `console.log( ___ )` のカッコの中だけを指で押さえ、文字通り読む**
- 練習19でこの手順を実行 → 1回目の予想で正解。4回続いたミスが手順を足しただけで解消
- これを予想のたびの癖にする

### 次回の続き
- JS土台づくり⑦：オブジェクト（`{ }`、名前付きの値の集まり）。`data.bitcoin.jpy` の正体
- 引き続き「カッコの中を先に読む」手順を徹底。js-practice.html を使う

---

## Day 45 — JS土台づくり⑦：オブジェクト・入れ子・配列×オブジェクト×ループ

### やったこと
- js-practice.html で練習20〜23
- オブジェクトの基本、入れ子オブジェクト、配列の中のオブジェクト、それをループで回すパターン

### 学んだこと

**1. オブジェクトの基本**
- `{ キー: 値, キー: 値, ... }` で「名前付きの値の集まり」を作る
- 配列との違い：配列は番号でアクセス（`nums[0]`）、オブジェクトは名前（キー）でアクセス（`user.name`）
- ドット記法でアクセス：これまで見てきた `console.log` `prices.map` `document.getElementById` も同じパターン（左がオブジェクト、右がそのキー）
- 番号より名前のほうがコードの意味が読める（`user.name` vs `user[0]`）

**2. 入れ子オブジェクト（`data.bitcoin.jpy` の正体）**
- 値の中にオブジェクトを入れられる。アクセスはドットを重ねる：`coin.price.jpy`
- ドットの連なりは「道順」。最終的に取り出されるのは **道の最後の値だけ**
- 練習21のミスから学び：「coin.price.jpy が "bitcoin 14000000" を返す」と予想したが正解は 14000000 だけ
- 例え：住所「東京→千代田→1-1」をたどっても、届くのは最終地点だけ。途中の地名は答えに含まれない
- **書いていないものは出てこない**（コードは書いてある通りに動く）

**3. 配列の中のオブジェクト（`COINS` の正体）**
- `[ { ... }, { ... }, ... ]` の形。各要素が同じ形のオブジェクト
- アクセスは「番号 → キー」の2段：`COINS[0].id` で「0番目のオブジェクトの id」
- 練習22：4つとも1回目で正解（btc, 30, xrp, 3）

**4. 配列×オブジェクト×ループ（最頻出パターン）**
- 練習23：`for (let i = 0; i < coins.length; i++) { console.log(coins[i].id); }` → btc, eth, xrp, done
- 「複数のデータを1つずつ処理する」典型。prices.html の4通貨ぶんの行作成・合計計算・スライス描画は全部この型の応用
- `COINS.map(coin => ...)` の `coin` も毎周 `COINS[0]` `COINS[1]` ... が入る。map は for ループの短い書き方（次回扱う）

**5. 「カッコの中を先に読む」手順、今日も有効**
- 練習20・22・23 は1回目の予想で正解。練習21だけ意味を込めすぎた予想ミス（修正）
- 手順そのものは定着してきている

### 次回の続き
- JS土台づくり⑧：関数（function）。定義と呼び出し、引数と戻り値、`drawLineChart` の正体
- その先：配列メソッド（map など）→ 実コードを読む段階へ

---

## Day 46 — JS土台づくり⑧：関数（定義・呼び出し・引数・戻り値・複数引数）

### やったこと
- js-practice.html で練習24〜26
- 関数の中核——定義/呼び出し・引数・戻り値・複数引数——を1日で通した
- 全問1回目で正解

### 学んだこと

**1. 定義と呼び出し（Day 27の復習）**
- 定義：`function 名前() { ... }`。**この時点では中の処理は走らない**。「こういう関数があるよ」と JS に教えるだけ
- 呼び出し：`名前()`。カッコが「実行ボタン」。中の処理が実際に走る
- 関数の中身が終わると、呼び出した次の行に戻る（順次実行のルールはそのまま）

**2. 引数（argument）**
- 定義の `( )` に受け取り用の名前、呼び出しの `( )` に渡す値
- 呼び出すたびに受け取る値が変わるので、同じ関数で違う計算ができる
- 練習24：`showDouble(5)` で 10、`showDouble(10)` で 20

**3. 戻り値（return）— 関数の本来の使い方**
- `return X;` で **呼び出し元に値を返す**
- 関数の呼び出しが **1つの値そのもの**になる：
  - 変数に入れられる：`let a = double(5)`
  - 計算に使える：`double(7) + 1` は `14 + 1`
  - 直接 console.log に渡せる
- 「式は内側から計算」の応用（Day 40の式の話の続き）。`3+2` が値 5 になるのと同じ感覚で、`double(5)` も値 10 になる

**4. 複数の引数**
- 何個でも受け取れる。`function add(a, b)`、`add(3, 5)` で a=3, b=5
- **位置で対応**（順番が決まり方）。名前で対応するわけではない
- 練習26：`add(sum, 100)` のように、変数や別の関数呼び出しの結果を引数にできる

### 進捗
- 「カッコの中を先に読む」手順が継続的に効いている。今日は3問すべて1回目正解
- これで `drawLineChart()` `loadPrices()` などの構造の中核（定義・呼び出し・引数・戻り値）は読める土台ができた

### 次回の続き
- JS土台づくり⑨：アロー関数（`p => p * 2` の短い書き方）と配列メソッド `map`
- その後：実コードを読む段階（prices.html / chartjs-test.html を土台の力で読み直す）
- 5/31ゴール「JS×AI」基準②（AIのJSを検証）に近づく段階

---

## Day 47 — JS土台づくり⑨：アロー関数・配列メソッド map

### やったこと
- js-practice.html で練習27〜29
- アロー関数の形・なぜ const に入れるか・map の動き・配列×オブジェクト×map の型

### 学んだこと

**1. アロー関数の形（普通の関数との対応）**
- 普通：`function double(x) { return x * 2; }`
- アロー：`const double = (x) => x * 2;`
- 変わった点：`function` → `const ... =`、引数の後ろに `=>`、文末に `;`
- 変わらない点：引数 `(x)`・呼び出し方 `double(5)`・戻り値の挙動
- 省略形：中身が `return` 1行だけなら `{ }` と `return` を省ける（`=>` の右の値がそのまま戻り値）

**2. なぜ function でなく const なのか（ユーザーからの良問）**
- 歴史的経緯：JSには元々 `function` しかなかった。アロー関数は後から「**名前を持たない式**」として追加された
- `(x) => x * 2` は「2倍する処理という値」そのもの。再利用したいなら箱（const）に入れる
- 本来の主用途は **名前を付けずにその場で渡す**：`nums.map(x => x * 2)`
- `map` のような「関数を引数で受け取るメソッド」に短く渡すために生まれた書き方

**3. map の動き（for ループの短縮形）**
- `配列.map((要素) => 処理)` で「各要素に処理をして新しい配列を作る」
- 配列の要素数だけ実行される。引数（`x`・`p`・`u` など）には毎回各要素が順番に入る
- for ループの `nums[i]` が引数の箱に置き換わっただけ。`i` を自分で管理しなくていい

**4. 配列の中のオブジェクトを map で回す型**
- 練習29：`users.map((u) => u.name)` で `["Alice", "Bob", "Carol"]`
- 「**オブジェクトの配列から、特定のキーの値だけを取り出した配列を作る**」最頻出パターン
- prices.html の `COINS.map(coin => ...)` と同じ型（変数名・引数名が違うだけ）

**5. 小数の精度問題（map とは別の話・予想外の発見）**
- 練習28で `100 * 1.1` の結果が `110.00000000000001` と表示された
- 原因：コンピュータは内部で2進数。`1.1` は2進数でピッタリ表せず、誤差が出る
- `300 * 1.1` がきれいに `330` になったのは、たまたま誤差がゼロに丸まったから
- 実務での対処：`.toFixed(2)` などで桁数を丸める。今は「そういうクセがある」と知っていればOK

**6. 配列を console.log した時の表示形式（復習＋確認）**
- `(3) [110.00000000000001, 220.00000000000003, 330]` のように **角括弧で1個の配列としてまとめて表示**
- `(3)` は length。三角▼で展開すると `0: 〜, 1: 〜, 2: 〜` と番号付きで見える

### 進捗
- 練習27（アロー関数の呼び出し）：3問とも1回目正解
- 練習28（map で2倍）：引数の動き・実行回数とも正解。表示の形と小数誤差は予想外で発見
- 練習29（配列×オブジェクト×map）：3問とも1回目正解。Day 45 の知識と接続できた
- ユーザーから「const は定数では？」「function ではなく const なのはなぜ？」の鋭い質問。仕組みを掘り下げて理解する姿勢が定着

### 次回の続き
- Day 48：アロー関数の本体が複数行（`(x) => { ... }`）の場合
- ここで JS 土台シリーズひと区切り
- その後：**実コード読み段階**（prices.html / chartjs-test.html を今までの知識で読み直す）→ 5/31 ゴール「JS×AI」の基準②（AIのJSを検証）へ

---

## Day 48 — JS土台づくり⑩：アロー関数の複数行ブロック・toFixed・引用符

### やったこと
- js-practice.html で練習30〜31
- アロー関数の本体が `{ ... }` ブロックの場合・小数誤差への対処（.toFixed）・シングル/ダブル引用符の違い
- JS 土台シリーズ（Day 39〜48）ひと区切り

### 学んだこと

**1. アロー関数の複数行ブロック**
- 省略形：`(x) => x * 2`（1個の式が自動的に戻り値）
- ブロック形：`(x) => { return x * 2; }`（`{ }` の中で `return` を書かないと値が返らない）
- ブロック形は「中で複数のことをしたい」とき。`const tax = ...` のような中間変数や複数の処理を書ける
- `return` を書き忘れると `undefined` が返るのがハマりどころ（普通の function と同じルール）

**2. `items` と `item` の区別（核心の慣習）**
- ユーザーから「item と items の区別が曖昧」と質問。命名慣習を整理して伝えた
- `items`（複数形 s 付き）= 配列全体、`item`（単数形）= 1個の要素
- `map((item) => ...)` の `item` には毎回オブジェクトまるごとが入る。`item.price` がその中の値
- 同パターン：`nums`/`x`、`prices`/`p`、`users`/`u`、`COINS`/`coin`
- この区別ができると、配列と要素の往復で混乱しなくなる

**3. 練習30での発見：文字列連結で小数誤差が表面化**
- `total = 100 * 1.1` → `110.00000000000001`（数値）
- `item.name + "は" + total + "円"` で連結すると、数値が文字列化されて `"りんごは110.00000000000001円"` に
- 練習28（数値の表示）で見た誤差が、練習30（文字列連結）でも同じ仕組みで顔を出した

**4. `.toFixed()` で見た目を整える**
- `total.toFixed(0)` で `"110"` になる（小数点以下0桁の文字列）
- ポイント：**返り値は文字列**（数値ではない）。Console で引用符付きで表示される
- 「数値の見た目を整える＝表示用」の定型パターン。実務頻出
- 練習31で `"りんごは110円"` のようにきれいに揃った

**5. 型（number と string）の見分け方**
- 数値は引用符**なし**：`110`
- 文字列は引用符**あり**：`'110'` または `"110"`
- 同じ「110 みたいなもの」でもコンピュータから見ると別の型
- 数値同士の `+` は加算、文字列が絡む `+` は連結（`"110" + 5` は `"1105"`）

**6. 引用符の3種類（ユーザー質問への回答）**
- シングル `'` とダブル `"` は **機能的に同じ**。プロジェクト内で統一されていればどちらでもOK
- 使い分けの理由：中に引用符が出る文字列を書きやすくするため。反対種類で囲めばエスケープ `\` を避けられる
- 慣習：多くのスタイルガイドはシングルを推奨。Console の表示もシングルがデフォルト
- 3つ目の囲み記号：**バッククォート `` ` ``**。`` `${name}は${price}円` `` のように `${}` で変数を埋め込める（テンプレートリテラル）。実コード読みで出てくる予定

### 進捗
- 練習30：item と items の区別で曖昧さが残ったが質問してくれた → 慣習として整理し納得
- 練習31：`.toFixed(0)` で整える流れを理解。値は完璧、型の意識は今後の課題
- 引用符の質問は本質的（機能/慣習/3つ目のバッククォート）。掘り下げる姿勢が継続

### JS 土台シリーズ（Day 39〜48）まとめ
| Day | テーマ |
|---|---|
| 39 | 変数・順次実行・console.log |
| 40 | 式（`x = x + 1` の仕組み） |
| 41 | 真偽値・比較・if/else |
| 42 | else if の順番・while |
| 43 | for ループ |
| 44 | 配列 |
| 45 | オブジェクト・入れ子・配列×オブジェクト |
| 46 | 関数（定義・引数・戻り値） |
| 47 | アロー関数・map（1行） |
| 48 | アロー関数の複数行ブロック・toFixed・引用符 |

→ JS の実行を1行ずつ追える土台がそろった

### 次回の続き
- **実コード読み段階に突入**：prices.html / chartjs-test.html を、これまでの土台で読み直す
- 5/31 ゴール「JS×AI」基準②（AIのJSを検証）に向けて
- 今までの予想→確認スタイルを実コードに対しても適用する

---

## Day 49 — 実コード読み開始：chartjs-test.html drawLineChart 前半（行14〜18）

### やったこと
- JS土台シリーズを終え、実コード読み段階に突入
- chartjs-test.html の drawLineChart() を「観察→予想→確認」スタイルで読み始めた
- 全体俯瞰 → 塊A（行14〜16: データ取得）→ map の中身（行17〜18）まで完了
- 塊B（Chart.js コンストラクタ）の途中で「同名問題」にぶつかり爆発前に終了

### 学んだこと

**1. 実コードの俯瞰スキル（最初に判明した強み）**
- ユーザーは全体を読んで「最終的に折れ線グラフを描く」「最後の行は関数を実行している」「22-28は見た目を整えている」と即答
- 不明行を自分で特定できる（14, 15, 16, 20, 21）。「全部わかる必要はなく、役割で塊として把握」が既にできている
- 17-18の map もすでに読めていた（Day 47 の力）

**2. p の中身の推理（Day 48 の items/item パターンの再適用）**
- `prices.map(p => p[1])` で「p は日付」と誤推理
- 修正：`p[0]` と `p[1]` の両方が使われている → p 自体が配列 `[日付, 価格]` のペア
- prices は「配列の中に配列」（CoinGecko API の形）
- map は配列の長さを変えない → labels も data も90個・同じインデックスでペアになる

**3. 「変数名は単なる名前」の確認質問**
- ユーザーから「const/let のあとの文字列はただの文字列か、意味があるか」と質問
- 答え：JS にとっては単なる識別子。同じコードを `xxx`/`yyy` に置き換えても動く
- 慣習で意味のある名前を付ける（res=response, ctx=context など）

**4. JSON の正体（再確認）**
- ユーザーから「JSON て何だったか」と質問
- データを文字列で表現する標準フォーマット。サーバーと JS の間でデータをやり取りする際に使う
- `fetch(url)` → JSON 文字列が届く → `.json()` で JS オブジェクトに開封
- 変数名 `json` と メソッド名 `.json()` が**同名なのが混乱の元**だった、と本人が言語化

**5. async / await / fetch（塊A）**
- 待つ必要のある処理（ネット越し・データ変換）には `await`、メモリ上の操作（プロパティアクセス）には不要
- `await` を使う関数には `async` を付けるルール
- ユーザーは確認問題2問とも正解：「16はすぐ終わるから await 不要」「14を await なしにすると返事が来る前に15で .json が走る」

**6. 「同名問題」で爆発寸前（重要な学習体験）**
- `labels: labels` `data: data` のように、Chart.js のキー名と自分の変数名が同じ
- 左＝Chart.js が固定で決めている項目名、右＝自分で作った変数。役割が完全に違う
- 例え：宅配伝票の「お届け先」欄（項目名）と書き込む住所（自分の情報）の関係
- ユーザー：「同じ文字列で役割が違うのが分かりづらく、もやもやがイライラに変わる」と率直に表明
- 設計文化の問題（理解力ではない）と確認。爆発前にセッション終了の判断

### 進捗
- JS土台シリーズの読解力が実コードに乗り始めた
- 「全体俯瞰 → 塊分け → 細部」の読み方が自然にできている
- 確認質問を躊躇なくぶつけられる姿勢が継続（変数名・JSON）
- 爆発前にやめる判断ができた（学習継続性が長期的に効く）

### 次回の続き（Day 50 の方針）
- chartjs-test.html を **「左＝Chart.jsのキー」「右＝自分の変数」が別の文字列**になる形に書き換える
  - 例：`labels: xLabels`、`data: yValues`
- 動作が同じことを確認してから、なぜ慣習で同名にするのか（読みやすさ）を扱う
- もやもや解消 → 慣習の理解、という順番
- その後：行20〜32 の Chart.js コンストラクタの構造（type, data, datasets, 各オプション）

---

## Day 50 — chartjs-test.html 後半：同名問題の解消・Chart.jsコンストラクタ構造・インデント整形

### やったこと
- drawLineChart() の変数を別名に書き換え（`labels`→`xLabels`、`data`→`yValues`）して「同名問題」を解消
- Chart.js コンストラクタの入れ子構造（type / data / labels / datasets）を読み切った
- 入れ子の `data` 2つ（階層違い）を区別
- ファイルのインデントと `=>` 前後のスペースを整形

### 学んだこと

**1. 同名問題①：左＝Chart.jsのキー、右＝自分の変数**
- `labels: xLabels` の形に書き換えると役割が一目で分かる（左はChart.jsが固定、右は自分の持ち物）
- 変数名を変えても動きは同じ（Day49「変数名は単なる名前」の実証）
- 左（キー）は変えられない、右（変数）は定義行と一緒なら変えられる
- なぜ慣習で同名にするか：①対応を考える手間が減る ②左右同名なら `{ labels }` のショートハンド記法が使える
- 別名・同名どちらも正解。本人は今は別名の方が読みやすい → 自分のコードは別名でOK

**2. 同名問題②：入れ子の違う階層に同じキー名（行22 data と 行26 data）**
- 行22の `data` = データ設定**全体**のフォルダ（中に labels と datasets）
- 行26の `data` = その中の**1本の線のY値だけ**
- 場所（入れ子の深さ）で意味が変わる。フォルダ構造の例えで整理
- 昨日は行26の右も `data`（`data: data`）で、実質3つの data が重なって爆発寸前だった、と本人が振り返り

**3. datasets が配列 `[ ]` の理由**
- `[ ]`（配列）＝線を何本でも入れられる入れ物、`{ }`（オブジェクト）＝1本の線の全情報
- 本人は最初 `{ }` の中身（凡例・値・色）を答えた → `[ ]` の役割（複数並べられる）に修正
- Day45「配列の中のオブジェクト」と同じ構造

**4. ETHの線を追加する思考（データの流れまで考えられた）**
- datasets配列に `{ }` を2個目として足す（前にカンマ）。インデントは下がらず横に並ぶ
- 重要な気づき：URL（13行目）は1通貨ぶんしか返さない → ETH用に url/fetch/json/map を別に追加が必要
- 「グラフに線を足す」は見た目だけでなく、その線のデータをどこから取るかまで考える話

**5. インデントは人間のため（JS は空白を見ない）**
- 本人が「行20の開きと行31の閉じの高さが合っていない」と自力で発見（HTML/CSSで養った構造把握力）
- JS はインデントを完全に無視＝動きは変わらない。100%読みやすさのため
- 揃えると入れ子の深さが目で見える（段差＝箱の深さ）
- `=>` の前後スペースも同様：慣習で両側に1個。`=>new` は詰まって誤読しやすい

### 進捗
- 「分からない」とした行（14,15,16,20,21）が全部クリア。drawLineChart() 全行読破
- 同名問題を「別名で動かす→慣習を理解」の順で解消（前回の方針通り、本人に合っていた）
- 確認問題の精度が高い。データの流れ・入れ子構造・視覚的整形すべてに気づけた

### 次回の続き
- drawPie()（円グラフ、行36〜57）を自力で読む練習 → drawLineChart とほぼ同じ構造なので力試しに最適
- または prices.html（257行、自作グラフ版）を土台の力で読み直す
- 5/31 ゴール「JS×AI」基準②（AIのJSを検証）が射程に入ってきた

---

## Day 51 — drawPie() 自力読み・スケジュール照合・③へ向けた計画

### やったこと
- chartjs-test.html の drawPie()（円グラフ・行36〜57）を、解説最小・予想主軸で自力読み
- 5/31ゴールに対する現在地をスケジュールと照合し、残り2日の計画を決定

### 学んだこと（drawPie 自力読み）

**1. 全体俯瞰（自力で的確）**
- 「円グラフを描く」「lineと同じ＝取得→整形→new Chart→表示」「違い＝取る数字が各通貨4つだけ・グラフ種類」と即答
- データの性質の違いを見抜いた：line=1通貨90日（時系列）、pie=4通貨の今1つずつ（比較）

**2. データ取得（部分A）**
- URL の `ids=bitcoin,ethereum,ripple,solana` で4通貨指定、とIDE上でピンポイント特定
- 変数名が json→data でも問題ないと即答（変数は書き手の自由）

**3. データ整形（部分B）— lineとの最大の違い**
- `caps` = 4通貨の時価総額の配列。用語補正：jpy_market_cap は「価格」でなく時価総額（cap=capitalization）
- `data.bitcoin.jpy_market_cap` のドットの道順を Day45 の読み方で分解。返るのは最終地点の数値1個
- なぜ map でなく手書き `[ ]` か → 「4個だけだから読み手に分かりやすく」と正解。「少数は手書き・多数は map」の使い分け

**4. 描画（部分C）**
- canvas が別id（myChart/pieChart）で描き先を分けている
- labels の役割＝各データの名前。type によって表示場所が変わる（line=X軸目盛り、pie=凡例）
- `data: caps` を Day50 の「左=Chart.jsキー・右=自分の変数」で正しく説明

### 進捗：実コードを読める段階に到達
- 解説最小で drawPie 全行を読み切った。drawLineChart で得た構造（async/fetch/json→整形→new Chart入れ子）が別関数でも通用すると実証
- 基準①（実行を追える）達成済み、②（AIのJSを検証）も今日の自力読みでほぼ達成

### スケジュール照合（5/29時点・ゴール5/31まで残り2日）
- フェーズ「実コード読み・AI協働」（5/26〜5/31）の進行中
- ゴール3基準：①達成・②ほぼ達成・**③（AIに指示する側）が未着手＝残る穴**
- 計画決定：
  - 5/30・5/31 で③に取り組む（題材有力：drawLineChartにETHの線を足す。本人が指示→AIが実装→本人が検証）
  - 6月以降は5/31ゴール後に方向再検討。prices.html読みは必要に応じて回す
- 補足：schedule.html の TODAY が 5/24 のまま（実際5/29）。要更新だが未着手（スコープ外のため触らず）

### 次回の続き（Day 52 = 5/30）
- 基準③に着手。本人が「作りたいJSの動き」を実装できる粒度で言語化 → AIが実装 → 本人が出力を検証（②③同時）
- 第一候補：drawLineChart に ETH の折れ線を追加（ETH用のurl/fetch/json/map追加 + datasets に2本目の{}）

---

## Day 52（5/30・土）— ③着手：AIに指示する側を実体験。1日で2サイクル完走・5/31ゴール実質達成

### やったこと
- 基準③「AIが実装できる粒度で指示できる」に初めて取り組んだ
- chartjs-test.html の drawLineChart に ETH の線を追加する題材で、1日で2サイクルまわした
- 役割が逆転：本人が指示を書く → AIが実装 → 本人が検証

### サイクル1：ETHの線を追加

**指示（本人）**：
> linechartにethereumのデータを追加する。coingeckoからのデータの取得、fetchする期間、データの整形はBitcoinと同じ。labelは'ETH価格'、色は青系。

**指示の質**：
- 「どこを・どう変えるか」を構造として書けていた（取得・整形・追加先・色）
- 「Bitcoinと同じ」で固定部分を明示、「青系で任せる」で裁量範囲を明示 → **固定/裁量の切り分けができていた**（実務で最も効くスキル）
- 私からの確認2点（xLabelsを使い回すか、青の具体色）に即答

**実装**：ETH用の ethUrl/ethRes/ethJson/ethPrices/ethValues を BTC と同じ型で追加、datasets に2本目の{}（borderColor `#3b82f6`、background `rgba(59,130,246,0.2)`）

**検証**：本人がブラウザで表示・凡例・エラー有無を確認 → 想定通り

### サイクル2：「コードは正しいが意図とズレる」を本人が発見し再指示

**本人の気づき（最重要）**：
> ETHのグラフをBTCのグラフに追加したため、Y軸の目盛りはBTCのまま。BTCとETHでは価格帯が大きく違うので、結果としてETHのグラフはほぼ直線になっている。コードは正しいのだけどね。

これが③の本質：「指示が実装可能な粒度だった（コードは正しい）」が「実際に作りたかったもの（比較したい）」とズレた、と自分で発見。実務でAIと協働するとき、最も大事な検証スキル。

**再指示**：
> Y軸を2本にし、左にBTCの目盛り、右にETHの目盛りとする。BTCとETHの線の色をpiechartの色に合わせる。

**実装**：
- 色を piechart に統一（BTC=`#f7931a` オレンジ、ETH=`#627eea` 青紫）
- 各datasetに `yAxisID: 'y'` / `'y1'` を追加
- 新出 `options.scales` で y（left）/ y1（right）を定義、y1 は `grid: { drawOnChartArea: false }` で横線重複を回避

**検証**：左軸BTC・右軸ETHの目盛り、色一致、ETHが直線でなく上下する、エラーなし → すべて意図通り

### 学んだこと

**1. 指示の粒度＝固定/裁量の切り分け**
- すべてを細かく指定するのではなく、「同じ」「任せる」を使い分けて指示すると効率的
- AI側が判断に詰まったら確認質問してくる → それに答えれば実装可能になる

**2. 「コードが正しい」と「意図通り」は別物**
- 実装が指示通りでも、見た目・使い勝手で意図とズレることがある
- ズレに気づいたら指示を補強して再実装。**往復で詰めていくのが現実**
- 一発で意図通りを期待しない。検証→再指示のループを前提に考える

**3. Chart.js の主要3要素（type / data / options）の全体像**
- type：グラフの種類
- data：描くデータ（labels と datasets）
- options：描き方の細かい設定（scales など）
- `options.scales.y` `y1` で複数軸を定義、データセットの `yAxisID` で紐付け

**4. 階層理解の深化**
- Day 50 の入れ子の読み方が `options.scales.y1.grid.drawOnChartArea` のような深い階層でも通用すると確認

### 進捗：5/31ゴール実質達成（残り1日を残して）
| 基準 | 状態 |
|---|---|
| ① 実行を追える | 達成（土台＋drawLineChart/drawPie） |
| ② AIのJSを検証 | 達成（drawPie自力読み＋今日の2回の検証） |
| ③ AIに指示できる | **達成（今日の2サイクル、特にサイクル2の再指示）** |

### 次回（Day 53 = 5/31・ゴール日）の計画
- **午前**：もう1サイクル ③（drawLineChart のグラフの見た目をもうひと工夫。本人の改善案を指示として実装）
- **午後**：6月以降の方針相談（Day 39〜52 の総括＋次のテーマ決め。prices.html 読みは必要に応じて）

---

## Day 53（5/31・日・ゴール日）— ③サイクル3完走・6月以降の方針決定・スケジュール再編

### やったこと
- 午前：③サイクル3（chartjs-test.html drawLineChart の見た目改善）
- 午後：4-5月の総括＋6月以降の方針相談＋スケジュールファイル再編成

### 午前：③サイクル3（4項目を1指示で）

**指示（本人）**：
> BTCの目盛りを10000000から14000000にする。グラフの値の線をもう少し細く。それぞれのグラフにタイトルを表示。折れ線グラフは「主要通貨の価格の推移」円グラフは「主要通貨の時価総額」

**実装**：
- BTC y軸に min/max 追加（10M-14M固定）
- 線を細く：borderWidth: 2（後で本人の指摘で「線でなくドットを小さく」に方針変更）
- 両グラフに title 追加（options.plugins.title）

**本人の補正指示（さらに改善）**：
> ETHの目盛りも300000から500000に変更したい。線を細くするより値のドットを小さくすればよかった。タイトルのフォントをもっと大きくしたい。

**再実装**：
- y1 に min/max 追加（300K-500K固定）
- borderWidth削除 → pointRadius: 2 に切り替え（本人の発想転換に従う）
- title.font.size: 18 で両グラフのタイトル拡大

**学んだこと**：
- `options.plugins.title.font.size` でタイトルフォント制御
- pointRadius でデータ点の大きさ調整、borderWidth と区別
- options 下に plugins と scales の2系統がある（タイトル/凡例 vs 軸）

**指示の質の進化**：
- 「線を細く」→「線でなくドットを」と自分で気づき方針変更できた
- 「コードは正しいが意図とズレる」を Day 52 サイクル2 で覚えた感覚が今日も働いた

### 午後：6月以降の方針

**本人の自己評価**：
> 手応え：自信がついた。HTML/CSS/JSの大まかな仕組みがわかった。AIへの指示方法・対話で詰めていくイメージが湧いた。

**重要な目標再整理**：
> 当初の2目標（自分で書ける／AIに指示できる）は「**入口**」だった。その先のゴールは「**AIに指示して楽しい・役に立つ・便利なものを作る**」。新領域（フレームワーク等）にも進んで発想を広げたい。

**Phase 1 → Phase 2 構造の合意**：
- Phase 1（景色を見る）：フレームワーク Astro/Svelte/React（+任意 Vue）を順に覗く＋既存Webアプリ実例観察
- 移動期間：スマホで継続（4月の出張パターン踏襲）
- Phase 2（プロジェクト）：Phase 1 の景色を踏まえて「何を作るか」を決定し実装開始
- 候補：初心者向けHTML/CSS/JS学習ツール（本人の復習を兼ねる）。ただし Phase 1 で変わる可能性あり

**スケジュールファイル再編**：
- schedule.html → schedule_202604.html にリネーム＋完結化（Level C 達成バッジ、全タスク done、TODAY「達成 5/31」、title「学習スケジュール（4-5月・完結）」）
- schedule_202606.html を新規作成
  - 期間：6/1〜8/31（当初9/30から短縮、6月を相対的に大きく見せる）
  - min-width: 2400px（物理サイズも拡大）
  - Phase 1 9タスク（各フレームワーク2分割：概要→チュートリアル＋例）
  - 移動 2タスク（スマホ継続・Phase 2 構想ノート）
  - Phase 2 6月分 3タスク（作るもの決定・技術選定・初期実装）
  - 7月以降 3タスク（rough表示：本格化・振り返り・次の探索）

**③のスキルが schedule 編集でも発揮された**：
- title 追加（タブで区別）、Phase 1 を広く・Phase 2 を狭く、入門の粒度を2分割、と細かい改善指示を本人が出した
- 「これでよい／もう少し細かく／別案？」で対話的に詰めていく、Day 52 のサイクル感覚がそのまま使えた

### 5/31ゴール達成・総括
| | 状態 |
|---|---|
| 当初目標①（自分で書ける） | 達成（「入口クリア」と本人が認識） |
| 当初目標②（AIに指示できる） | 達成（「入口クリア」と本人が認識） |
| Level A/B/C | 全達成 |
| JS×AI 3基準 | 全達成（①土台・②検証・③指示） |
| **真のゴール（本ゴール）** | **AIに指示して楽しい/役に立つ/便利なものを作る → 6月以降へ** |

4月から55日、土台が積み上がった。次は応用と創造へ。

### 次回（Day 54 = 6/1・月）の計画
- Phase 1 開始：Astro 概要・ドキュメント斜め読み（1日）
- schedule_202606.html を見ながら進める
- 「景色を見る」目的：何ができるかのレパートリーを増やし、Phase 2 の発想を広げる
